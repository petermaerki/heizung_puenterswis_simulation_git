{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run_simulation.py",
            "type": "shell",
            "command": "${command:python.interpreterPath} run_simulation.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "run_varianten.py",
            "type": "shell",
            "command": "${command:python.interpreterPath} run_varianten.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "test_speicher_dezentral.py",
            "type": "shell",
            "command": "${command:python.interpreterPath} test_speicher_dezentral.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        }
    ]
}