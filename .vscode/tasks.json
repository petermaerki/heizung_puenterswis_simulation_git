{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run_simulation.py (alle Varianten, reset)",
            "type": "shell",
            "command": "${command:python.interpreterPath} run_simulation.py --notebooks=0 --reset=1",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "run_simulation.py (alle Varianten)",
            "type": "shell",
            "command": "${command:python.interpreterPath} run_simulation.py --notebooks=0",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "run_simulation.py (ww_ladung_65C_1.1h)",
            "type": "shell",
            "command": "${command:python.interpreterPath} run_simulation.py --notebooks=0 --variante=ww_ladung_65C_1.1h",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "test_speicher_dezentral.py",
            "type": "shell",
            "command": "${command:python.interpreterPath} test_speicher_dezentral.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "build"
        }
    ]
}